public void shuffle(){
    for(int i = (array.length -1); i>0; i--){
      int rand = random.nextInt(array.length);
      card temp = array[rand];
      array[rand] = array[i];
      ar[i] = temp;
    }